<Page x:ConnectionId='1'
    x:Class="LMS_Project.Pages.ChapterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LMS_Project.Pages"
    xmlns:data="using:LMS_Project.Data"  
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Page.Resources>
        <DataTemplate                           x:Key="ChapterDataTemplate">
            <StackPanel x:ConnectionId='2' HorizontalAlignment="Stretch" Padding="0,10">
                <TextBlock x:ConnectionId='3' FontSize="21"                                 HorizontalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" TextAlignment="Center"/>
                <TextBlock x:ConnectionId='4' FontSize="18"                      HorizontalAlignment="Center" TextTrimming="WordEllipsis" TextAlignment="Center" />
                <TextBlock x:ConnectionId='5' FontSize="12"                            HorizontalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" TextTrimming="CharacterEllipsis" TextAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid x:ConnectionId='6' Background="{ThemeResource SystemControlBackgroundAltHighBrush}"                                                    >
        <ProgressRing x:ConnectionId='7' x:Name="LoadingIndicator"></ProgressRing>
        <TextBlock x:ConnectionId='8' x:Name="tbMessage" Text="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="25" CharacterSpacing="1"/>
        <ListView x:ConnectionId='9' x:Name="MainListView"
                  ItemsSource="{Binding}"
                  ItemTemplate="{StaticResource ChapterDataTemplate}" 
                  IsItemClickEnabled="true"             
                                                     Margin="0,0,0,100"                                       >
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        <Grid VerticalAlignment="Bottom" Height="100" Background="{ThemeResource SystemControlForegroundAccentBrush}">
            <TextBlock x:ConnectionId='10'                       HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="25" FontFamily="Sylfaen" MaxLines="2" TextWrapping="Wrap"/>
        </Grid>
    </Grid>
</Page>

